meta {
  name: Write Cocktail (Array items on 1 line)
  type: http
  seq: 8
}

post {
  url: {{host}}/drinks/cocktail
  body: json
  auth: inherit
}

body:json {
  {
    "bitters": ["Angostura", "{{fish}}"],
    "ice": true
  }
}
